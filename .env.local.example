# ===========================================
# Wanderlust Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# Convex Configuration
# -------------------------------------------

# Option 1: Convex Cloud (managed service)
# VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Option 2: Self-Hosted Convex
VITE_CONVEX_URL=http://localhost:3210

# Server-side Convex URL (for SSR in Docker)
# Only needed when running in Docker - set to internal network address
# CONVEX_URL=http://convex-backend:3210

# For self-hosted deployments, also set:
CONVEX_SELF_HOSTED_URL=http://localhost:3210
CONVEX_SELF_HOSTED_ADMIN_KEY=

# -------------------------------------------
# Maps (optional)
# -------------------------------------------

VITE_STADIA_API_KEY=
VITE_MAPILLARY_ACCESS_TOKEN=

# -------------------------------------------
# Convex Environment Variables
# -------------------------------------------
# These must be set via: npx convex env set <KEY> <value>
# Or for self-hosted: Use the Convex dashboard at http://localhost:6791

# Site URL (required for BetterAuth - your app's public URL)
# npx convex env set SITE_URL http://localhost:3000

# BetterAuth Secret (required - generate with: openssl rand -base64 32)
# npx convex env set BETTER_AUTH_SECRET <your-secret-here>

# OpenRouter API Key (for AI features - optional)
# npx convex env set OPENROUTER_API_KEY sk-or-your_openrouter_api_key_here
