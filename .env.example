# ===========================================
# Wanderlust Self-Hosted Configuration
# ===========================================
# Copy this file to .env and fill in your values

# -------------------------------------------
# Convex Self-Hosted Backend
# -------------------------------------------

# Public URL where Convex API is accessible (for browser/client)
# For local development: http://localhost:3210
# For production: https://api.your-domain.com
VITE_CONVEX_URL=http://localhost:3210

# Internal Convex URLs (for container-to-container communication)
CONVEX_CLOUD_ORIGIN=http://convex-backend:3210
CONVEX_SITE_ORIGIN=http://convex-backend:3211

# Dashboard URL (for browser access to Convex dashboard)
NEXT_PUBLIC_DEPLOYMENT_URL=http://localhost:3210

# Admin key for deploying Convex functions (generate after first start)
# Run: docker compose exec convex-backend ./generate_admin_key.sh
CONVEX_SELF_HOSTED_ADMIN_KEY=

# Convex deployment URL for CLI (same as VITE_CONVEX_URL)
CONVEX_SELF_HOSTED_URL=http://localhost:3210

# -------------------------------------------
# Database (Optional - defaults to SQLite)
# -------------------------------------------

# PostgreSQL connection URL
# POSTGRES_URL=postgresql://user:password@host:5432/convex_db

# MySQL connection URL
# MYSQL_URL=mysql://user:password@host:3306/convex_db

# Set to 1 if your database doesn't support SSL
# DO_NOT_REQUIRE_SSL=1

# -------------------------------------------
# S3 Storage (Optional - for file uploads)
# -------------------------------------------

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# S3_STORAGE_FILES_BUCKET=wanderlust-files
# S3_STORAGE_EXPORTS_BUCKET=wanderlust-exports

# For S3-compatible storage (MinIO, etc.)
# S3_ENDPOINT_URL=http://minio:9000
# AWS_S3_FORCE_PATH_STYLE=true

# -------------------------------------------
# Convex Settings
# -------------------------------------------

# Log level: error, warn, info, debug, trace
RUST_LOG=info

# Document retention delay in seconds (default: 172800 = 48 hours)
DOCUMENT_RETENTION_DELAY=172800

# -------------------------------------------
# Maps & External Services (Optional)
# -------------------------------------------

# Stadia Maps API key for map tiles
VITE_STADIA_API_KEY=

# Mapillary access token for street view
VITE_MAPILLARY_ACCESS_TOKEN=

# -------------------------------------------
# Better Auth (Authentication)
# -------------------------------------------

# Better Auth secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=

# Better Auth URL (your app's public URL)
BETTER_AUTH_URL=http://localhost:3000

# GitHub OAuth (optional)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Google OAuth (optional)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
